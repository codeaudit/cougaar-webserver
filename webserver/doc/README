Contains two packages:

  org.cougaar.lib.web.arch:
    Generic architecture for a ServletEngine and the
    registration of (path, Servlet) pairs within this
    engine.  Only depends upon "javax.servlet.*".

  org.cougaar.lib.web.service:
    Component implementations of core's 
    "org.cougaar.core.servlet.ServletService" API, which
    uses the above "org.cougaar.lib.web.arch" classes
    and core classes.

[Further documentation will be added in the near future]
